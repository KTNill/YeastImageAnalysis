# YeastImageAnalysis

本プロジェクトは、熊本大学の谷研究所において、酵母が生成・蓄積する油分量（脂質生産性）やその細胞状態を効率的かつ定量的に解析する研究を目的として開発された画像解析システムです。

透過光画像（BF）、蛍光画像（FL）、およびPI蛍光画像（PI）を組み合わせることで、酵母の細胞数、油脂蓄積・生産量、さらには壊死細胞割合までを自動で定量化するGUIアプリケーションとなっています。

内部のエンジンには Cellpose によるセグメンテーションを採用し、細胞領域・油脂領域・壊死領域を高精度に検出。解析結果を統計データ（CSV）や可視化された統合マスク画像として一括出力します。

## 主な機能

- 画像フォルダを選択しての一括自動解析
- 透過光画像（BF）からの細胞領域検出
- 蛍光画像（FL）からの油脂領域検出
- PI蛍光画像（PI）からの壊死領域検出
- 細胞内油脂量・総油脂量・細胞内外分布、および壊死細胞割合の自動算出
- 解析結果と統計サマリーをCSVとして一括保存
- 前処理適用画像、マスク重畳画像、細胞・油脂・壊死の統合画像の保存
- 目的別の設定ファイル（共通・細胞・油脂・壊死）による詳細なパラメータチューニング
- GUI上で解析ログと進捗のリアルタイム表示
- 単一画像を素早く試せる「1枚テストプレビュー」機能
- ファイル名や重要ログ、解析エラーのハイライト表示
- モデル設定や実行環境が変わらない場合の解析モデル再利用（高速化）

## ディレクトリ構成

```text
YeastImageAnalysis/
├── app/
│   ├── core/
│   │   └── analyzer.py          # 画像解析処理
│   ├── gui/
│   │   └── app_window.py        # GUI処理
│   └── utils/
│       └── config_loader.py     # 設定CSV読み込み管理
├── config/
│   ├── config_common.csv        # 共通設定
│   ├── config_cell.csv          # 細胞解析設定
│   ├── config_lipid.csv         # 油脂解析設定
│   └── config_necrosis.csv      # 壊死解析設定
├── logs/                        # 実行ログ出力先
├── models/                      # カスタムモデル配置先
├── tests/                       # テストコード
├── main.py                      # アプリ起動エントリポイント
├── requirements.txt             # Python依存関係
├── 起動ファイル.bat             # Windows用起動ファイル
└── MAC用起動ファイル.command    # macOS用起動ファイル
```

## 動作環境

本プロジェクトは Python で実装されています。主な依存ライブラリは以下の通りです。

- customtkinter
- cellpose
- torch
- torchvision
- pandas
- numpy
- opencv-python
- scikit-image

依存関係の正確なバージョン等は `requirements.txt` を参照してください。

## 起動方法

### Windows

プロジェクト直下のバッチファイルを実行します。

```text
起動ファイル.bat
```

この起動ファイルは、同梱の環境 `python_env` を使用して `main.py` を実行する仕組みです。
（実態: `.\python_env\python.exe .\main.py`）

### Pythonから直接起動する場合

自身で構築した仮想環境を有効化したうえで、以下を実行してください。

```bash
python main.py
```

## 設定ファイル

解析設定は `config/` ディレクトリ内の以下のCSVで機能ごとに管理されます。初回起動時、ファイルが存在しない場合はデフォルト設定で自動作成されます。GUI上の「⚙」ボタンからも直接編集・保存が可能です。

- `config_common.csv`: 共通設定（画像識別子、GPU使用の有無、統合画像の描画色など）
- `config_cell.csv`: 細胞検出に関する設定（モデルパス、細胞径、閾値、最小サイズなど）
- `config_lipid.csv`: 油脂検出に関する設定（モデルパス、ノイズカット、平均輝度閾値、ガンマ補正など）
- `config_necrosis.csv`: 壊死検出に関する設定（モデルパス、最小重複割合、ノイズカット、ガンマ補正など）

**主な設定項目（一部）:**

| 項目名 | 内容 | 該当ファイル |
|---|---|---|
| 細胞モデルパス | 細胞検出用のモデルパス（空欄でデフォルトのcpsam使用） | config_cell.csv |
| 油脂モデルパス | 油脂検出用のモデルパス（空欄でデフォルト使用） | config_lipid.csv |
| 壊死モデルパス | 壊死検出用のモデルパス（空欄でデフォルト使用） | config_necrosis.csv |
| 透過光画像識別子 | BF画像を識別する文字列（デフォルト:`_BF`） | config_common.csv |
| 蛍光画像識別子 | FL画像を識別する文字列（デフォルト:`_FL`） | config_common.csv |
| PI蛍光画像識別子 | PI画像を識別する文字列（デフォルト:`_PI`） | config_common.csv |
| GPU使用 | GPUを使用する場合は `1.0`、使用しない場合は `0.0` | config_common.csv |

## 画像ファイルの命名規則

各画像は設定CSVの識別子に基づいて自動でペアリングされます。**BF画像の識別子がファイル名末尾にあること**を基準として探します。

デフォルト設定の例:
```text
透過光画像識別子: _BF
蛍光画像識別子: _FL
PI蛍光画像識別子: _PI
```

ファイル構成の例：
```text
sample01_BF.tif  (細胞用)
sample01_FL.tif  (油脂用)
sample01_PI.tif  (壊死用)
```
※この場合、プレフィックス「sample01」が一致するこれら3つのファイルが1セットとしてまとめて解析されます。

対応する画像拡張子: `.png`, `.jpg`, `.jpeg`, `.tif`, `.tiff`

## 解析結果

解析を実行すると、選択した画像フォルダ内に以下の形式で結果保存用の新規フォルダが作成されます。（GUIの「実験メモ」に入力した文字列はフォルダ名に組み込まれます）

```text
result_メモ内容_YYYYMMDD_HHMMSS/
```

出力内容の例：
```text
result_test1_20260803_140000/
├── config_common.csv                  # 実行時の設定バックアップ
├── config_cell.csv                    # 実行時の設定バックアップ
├── config_lipid.csv                   # 実行時の設定バックアップ
├── config_necrosis.csv                # 実行時の設定バックアップ
├── analysis_summary.csv               # 解析結果と統計のCSV
├── 01_cell_sample01_BF.tif            # 細胞マスク画像
├── 02_lipid_sample01_BF.tif           # 油脂マスク画像
├── 03_combined_sample01_BF.tif        # 細胞・油脂統合画像
├── 02_necrosis_sample01_BF.tif        # 壊死マスク画像
└── 03_combined_necrosis_sample01_BF.tif # 細胞・壊死統合画像
```
※ ノイズカット等の前処理機能が有効な場合、`00_clean_...` などの前処理適用済み画像も合わせて出力されます。

### 出力CSVについて

`analysis_summary.csv` には、画像ごとの解析値と全体統計（平均・標準偏差）が出力されます。主な出力項目は以下の通りです。

- **ファイル名**: 解析対象の基本となるBF画像名
- **細胞解析**: 細胞数, 細胞面積(px)
- **油脂解析**: 油脂占有率, 総生産率, 細胞内油脂割合(%), 油脂保有細胞数, 油脂保有細胞割合(%)
- **壊死解析**: 壊死細胞数, 壊死細胞割合(%)

## 指標の定義

本システムでは、研究の評価軸となる各種指標を以下のように定義して算出しています。

```text
油脂占有率 (蓄積度)  = 細胞内の油脂総面積 / 細胞の総面積
総生産率 (効率)      = 全油脂面積(細胞外含む) / 細胞の総面積
細胞内油脂割合 (分布)= (細胞内の油脂面積 / 全油脂面積) * 100
油脂保有細胞割合     = (油脂を含んでいる細胞数 / 全体の細胞数) * 100
壊死細胞割合         = 壊死と判定された細胞数 / 全体の細胞数
```

## GUIの使い方

1. アプリを起動する。
2. `画像フォルダを選択` をクリックし、解析対象の画像群が入ったフォルダを選択する。
3. フォルダ名に使用される `実験メモ` を必要に応じて入力する。
4. 実行したい解析（細胞 / 油脂 / 壊死）にチェックを入れる。
5. 各項目の横にある `⚙` ボタンから、しきい値や使用モデルなどの詳細設定を確認・変更する。
6. `1枚テストプレビュー` をクリックし、プレビュー対象（前処理後 / マスク / マージ）を切り替えながら、パラメータが適切か確認・調整する。
7. 設定が完了したら `解析開始` をクリックする。
8. 進捗バーとログで解析状況を確認する。
9. 解析完了後、ログに表示された「解析結果保存先」フォルダを開きデータを確認する。

## 解析モデルの再利用機構

解析を開始するたびに設定CSVは再読み込みされますが、以下の「モデルロードに関わる設定」が前回と変更されていない場合は、既存の解析モデルをメモリ上に保持したまま再利用します。

- 細胞モデルパス
- 油脂モデルパス
- 壊死モデルパス
- GPU使用の有無

これにより、細胞径・閾値・最小サイズ・描画色などを微調整して再解析する際の「重いモデルロード時間」が省略され、瞬時に処理を開始できます。

## GPU利用について

`config_common.csv` の `GPU使用` の値により処理デバイスを切り替えます。

- `1.0`: GPUを使用（推奨）
- `0.0`: GPUを使用しない（CPUのみ）

CUDA環境が構築されている場合は自動でCUDAを優先します。また、macOS環境ではMPS（Metal Performance Shaders）が利用可能な場合にMPSを使用します。有効なGPU環境が見つからない場合は自動的にCPUフォールバックして解析を続行します。

## 注意事項

- BF画像、FL画像、PI画像は同一視野のものであり、サイズ（解像度）が完全に一致している必要があります。
- BF画像に対して、対応するFLやPI画像が見つからない場合、そのセットの油脂・壊死解析はスキップして出力されます。
- 解析中に特定の画像セットでエラーが発生した場合、アプリはクラッシュせずその画像をスキップし、次の画像の解析を継続します。
- プロジェクト内の `python_env` フォルダは「Windowsバッチ起動用」の実行環境として扱ってください。IDE（PyCharmやVSCode等）での開発用インタープリタとは分離することを強く推奨します。

## 開発時の注意

IDE環境でコーディングを行う際は、別途開発用の仮想環境（venv等）を構築し、BAT起動用の `python_env` フォルダの中身を誤って上書き・破壊しないようにしてください。

推奨運用：
```text
python_env : 配布・バッチ起動用の固定実行環境
venv       : IDEでの開発・テスト用環境
```
IDEやエディタのアップデート後などに、プロジェクトインタープリタが意図せず `python_env` を参照していないか定期的に確認してください。

## トラブルシューティング

### 「画像が見つかりません」と表示されて解析が始まらない
以下を確認してください。
- 選択したフォルダ内に画像ファイルが実際に存在するか。
- 拡張子が対応形式（.png, .tif, .jpgなど）になっているか。
- 設定した画像識別子とファイル名が完全に一致しているか。
- BF識別子がファイル名の「末尾（拡張子の手前）」に付いているか。

例：
```text
設定CSVの指定: _BF
有効なファイル名: sample01_BF.tif
無効なファイル名: _BF_sample01.tif, sample01_BF_copy.tif
```

### 実行時に `operator torchvision::nms does not exist` のようなエラーが出る
Python環境内で `torch` と `torchvision` のバージョン不整合が起きているか、インストールが破損している可能性が高いです。
バッチファイルで使用しているPython環境から以下を実行し、バージョンが正しく対応しているか確認してください。

```bat
.\python_env\python.exe -m pip show torch torchvision
.\python_env\python.exe -c "import torch; import torchvision; print(torch.__version__, torchvision.__version__)"
```
エラーが出る場合は、仮想環境を作り直すか、対応バージョンの再インストールを行ってください。

## 今後の課題 (TODO)

- **macOS環境での動作確認と修正**
  - 同梱されている `MAC用起動ファイル.command` は現在実機での検証が行えておらず、動作しない可能性が高いです。macOS環境での確実な動作確認およびパス解決の修正が必要です。
- **動的な閾値調整とリアルタイムプレビュー機能の追加**
  - 現在の仕様では「設定ウィンドウで保存 → 1枚テストプレビュー」という手順を踏む必要があります。今後は、GUI上にスライダー等を実装し、動的に閾値を変更しながらリアルタイムでプレビュー画像が更新される「インタラクティブなパラメータ調整機能」の追加を予定しています。